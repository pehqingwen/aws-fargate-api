{
  "type": "module",
  "name": "fargate-api",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "express": "^4.x",
    "pg": "^8.x",
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x"
  },
  "scripts": {
    "start": "node server.js"
  }
}